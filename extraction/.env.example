# =============================================================================
# Trace Pipeline - Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your values

# -----------------------------------------------------------------------------
# LLM Provider Configuration
# -----------------------------------------------------------------------------
# Groq API (default provider)
GROQ_API_KEY=gsk_your_groq_api_key_here

# Optional: Alternative LLM providers (uncomment to use)
# LLM_PROVIDER=openrouter  # Options: groq, openai, anthropic, openrouter
# OPENAI_API_KEY=sk-your_openai_key
# OPENROUTER_API_KEY=sk-or-your_openrouter_key
# ANTHROPIC_API_KEY=sk-ant-your_anthropic_key
# LLM_MODEL=meta-llama/llama-3.3-70b-instruct  # Model override

# -----------------------------------------------------------------------------
# Neo N3 Blockchain Configuration (for on-chain attestations)
# -----------------------------------------------------------------------------
NEO_NETWORK=testnet
NEO_PRIVATE_KEY=your_wif_private_key_here

# Optional: Custom RPC URL (defaults to official testnet/mainnet)
# NEO_RPC_URL=http://seed1.neo.org:10333

# Hypothesis Registry Contract (deployed to testnet)
# Leave blank to use simple attestation mode
NEO_REGISTRY_CONTRACT=

# -----------------------------------------------------------------------------
# NeoFS Configuration (SpoonOS Tool - Decentralized Storage)
# -----------------------------------------------------------------------------
# NeoFS endpoint for storing hypotheses
NEOFS_ENDPOINT=grpc://st1.storage.fs.neo.org:8080

# Path to NeoFS wallet JSON file
NEOFS_WALLET_PATH=

# Container ID for hypothesis storage (will be created if not set)
NEOFS_CONTAINER_ID=

# -----------------------------------------------------------------------------
# X402 Payment Configuration (SpoonOS Tool - Micropayments)
# -----------------------------------------------------------------------------
# Enable X402 payments for hypothesis minting
# Requires ETH wallet with USDC on Base Sepolia testnet

# Private key (0x-prefixed Ethereum private key)
X402_PRIVATE_KEY=

# Receiver address for minting payments
X402_RECEIVER_ADDRESS=

# Network (default: base-sepolia for testing)
X402_NETWORK=base-sepolia

# Minting fee in USDC (default: 0.001)
X402_MINT_FEE=0.001

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# - Get testnet GAS from: https://n3t5wish.ngd.network/#/
# - Get testnet USDC from: https://faucet.circle.com/
# - NeoFS documentation: https://fs.neo.org/
# - X402 protocol: https://xspoonai.github.io/docs/examples/x402-react-agent/
