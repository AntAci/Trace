# Trace: Scientific Paper Hypothesis Generation Pipeline
# Consolidated requirements for all phases
# 
# Python version requirement: Python 3.12 or higher is required
# Install Python 3.12+ before installing these dependencies

# Core LLM and API
groq

# Environment and configuration
python-dotenv

# PDF processing
PyPDF2>=3.0

# SpoonOS integration (required)
# - Provides Agent protocol (SpoonReactAI) for Phase 2 & 3
# - Provides Tool interface for Phase 1 & 4
# - Provides StateGraph for workflow orchestration
# - Includes NeoFS and X402 tools from spoon-core repository
# Install from git repository to get latest tools:
#   - neofs_tools.py: https://github.com/XSpoonAi/spoon-core/blob/main/spoon_ai/tools/neofs_tools.py
#   - x402_payment.py: https://github.com/XSpoonAi/spoon-core/blob/main/spoon_ai/tools/x402_payment.py
git+https://github.com/XSpoonAi/spoon-core.git@main

# Neo blockchain integration (included in spoon-ai-sdk)
# neo-mamba is automatically installed with spoon-ai-sdk
# For standalone installation: pip install neo-mamba
# neo-mamba

# Neo smart contract development (optional - for contract deployment)
# Uncomment if you need to compile/deploy contracts:
# boa3

# Additional dependencies (may be included in spoon-ai-sdk):
# - NeoFS storage tools: neofs-sdk (if not bundled)
# - X402 payment tools: web3, eth-account (if not bundled)

# API server (for web frontend)
fastapi
uvicorn[standard]
python-multipart

